# Variables de entorno para builds de Capacitor (iOS nativo)
# Copiá esto a .env.local antes de hacer "npm run build" para iOS
#
# Apunta al backend en Render (sin trailing slash)
VITE_API_URL=https://drift.onrender.com
